[package]
name = "dorobot-flex"
version.workspace = true
edition = "2021"
description = "DoRobot Studio with Flex Layout Shell"

[dependencies]
# Workspace dependencies
dorobot-types = { workspace = true }

# Makepad
makepad-widgets = { workspace = true }
# The shared visual language: tokens, faces and the widget kit.
dorobot-ux = { workspace = true }
makepad-app-shell = { workspace = true }
# Local WIP clone already ported to makepad dev; upstream still targets rik
makepad-urdf-player = { git = "https://github.com/dorobot-org/makepad-urdf-viewer", rev = "283d303df6d3e158bac2c281a190ac1f749b5bd5" }

# Logging
log = { workspace = true }
env_logger = { workspace = true }

# Data loading
parquet = { workspace = true }
arrow-array = "56"
arrow-schema = "56"
serde = { workspace = true }
serde_json = { workspace = true }

# File dialogs
rfd = { workspace = true }

# Error handling
thiserror = { workspace = true }
anyhow = { workspace = true }

# Video decoding (optional)
ffmpeg-next = { workspace = true, optional = true }

[features]
default = ["video"]
video = ["ffmpeg-next"]
